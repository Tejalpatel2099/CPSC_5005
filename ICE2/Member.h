#include <iostream>
#include <string>
using namespace std;

// class represents a member in system, where it stores information such as
// member name, and member ID, and aslo provides functionality to add new
// members, login and logout.
class Member {
private:
    string memberName; // Name of member
    int memberId; // Account number od member i.e id

public:
    Member() {} // Constructors

    // parameterized constructor
    Member(string name, int account) {
    }

    // Function to add a new member to the system
    void addNewMember(Member a) {

    }
    // Function to login a member into the system
    void login (Member b) {

    }
    //   Function to logout a member from the system
    void logout (Member c) {

    }
};


