#include <iostream>
# include <string>
# include "Book.h"
using namespace std;

// class represents a rating given by a member for book, where it stores
// information of rating value and provides functionality to view own ratings
// and to rate a book by a member.
class Rating {
private:
    int Rate;
public:
    Rating (int Rate) {

    }
    // Function to view all own ratings given by member
    void viewAllYourOwnRatings(Member R){

    }
    // Function to rate book  by a member
    void rateBook(Book p, Member q, Rating s) {

    }
};